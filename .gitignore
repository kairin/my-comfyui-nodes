.env
.env.*
!.env.example
.envrc.local
*.token
*.key
*.pem
*.log
*.sqlite
*.db
*.pyc
*.pyo
__pycache__/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Runtime and machine-local state.
.hermes/
tmp/
temp/
logs/
run/
user/
output/
input/
.tracking

# Model/checkpoint payloads should not live in this public repo.
*.safetensors
*.ckpt
*.pt
*.pth
*.bin
*.gguf
*.onnx

# Local custom configuration that may contain paths, prompts, or tokens.
custom_models.json
system_prompts_user.json
debug_config.json
debug_*.json

# Track Spec Kit skills, but keep future agent runtime/auth state out of Git.
.agents/*
!.agents/skills/
!.agents/skills/**

# Model registry generated compatibility views (runtime, disposable).
.comfygo_views/

# GC quarantine area (runtime only — never commit model data).
.comfygo_trash/

# Workflow diagnose checkpoints (future; runtime only).
.comfygo_debug/
