#!/usr/bin/env bash
set -euo pipefail

repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if [[ $# -eq 0 ]]; then
  exec "$repo_dir/scripts/comfy-local" go
fi

exec "$repo_dir/scripts/comfy-local" "$@"
